import { createTheme } from '@mui/material';
export const appTheme = createTheme({
  palette: {
    primary: {
      main: '#ffccbc',
      light: '#ffffee',
      dark: '#cb9b8c',
    },
    secondary: {
      main: '#f8bbd0',
      light: '#ffeeff',
      dark: '#c48b9f',
    },
    background: {
      default: 'white',
      paper: '#e1f5fe',
    },
  },
});
